# March 15, 2026

## Supabase keep-alive updates

- Added `heartbeat_pings` table (migration 024) so the scheduled job can insert a record when the workflow runs.
- Updated `scripts/keep-supabase-active.js` to POST to `/rest/v1/heartbeat_pings` instead of calling the nonexistent `/rpc/now` endpoint.
- Documented the table creation and heartbeat procedure in `supabase/README.md` so future engineers know how to reproduce the keep-alive setup.
