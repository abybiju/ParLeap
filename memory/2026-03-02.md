# March 2, 2026

## Supabase Security Advisor remediation

### Context
- Supabase advisor reported a critical issue (`Security Definer View`) and several warnings:
  - Function Search Path Mutable
  - Extension in Public (`public.vector`)
  - Leaked Password Protection Disabled (auth setting)
  - Auth RLS Initialization Plan advisories

### Work completed
- Added migration `supabase/migrations/022_security_advisor_fixes.sql`
  - Set `public.template_stats` to `security_invoker = true`
  - Hardened search_path on key functions:
    - `public.update_updated_at_column`
    - `public.increment_template_usage`
    - `public.match_songs`
    - `public.match_songs_by_embedding`
  - Added best-effort vector extension schema move to `extensions`

- Added migration `supabase/migrations/023_rls_init_plan_optimizations.sql`
  - Recreated RLS policies using `(select auth.uid())` / `(select auth.role())`
  - Covered: `profiles`, `songs`, `events`, `event_items`, `community_templates`, `template_votes`, `bible_versions`
  - Added indexes: `community_templates(created_by)`, `template_votes(user_id)`

- Guided auth hardening in dashboard:
  - Enabled secure password change
  - Verified leaked-password protection location (plan-limited on Free)

### Result
- User confirmed the fixes worked in Supabase advisor.
- Remaining items are mostly plan-limited or non-critical performance advisories.
